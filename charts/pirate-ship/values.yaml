radarr:
  enabled: false
  metrics:
    enabled: false
  serviceMonitor:
    metrics:
      enabled: false
  persistence:
    media:
      enabled: false
    config:
      enabled: false

sonarr:
  enabled: false
  metrics:
    enabled: false
  serviceMonitor:
    metrics:
      enabled: false
  persistence:
    media:
      enabled: false
    config:
      enabled: false

transmission:
  enabled: false
  persistence:
    config:
      enabled: false

nzbget:
  enabled: false
  persistence:
    config:
      enabled: false

overseerr:
  enabled: false
  persistence:
    config:
      enabled: false